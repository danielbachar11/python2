name: Docker Login

on: pull_request

jobs:
  docker-login:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v3
       


   

      - name: build image
        
        run: docker build -t python:${{ github.sha}} .
