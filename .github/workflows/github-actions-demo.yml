name: Docker Login

on: push

jobs:
  docker-login:
    runs-on: arc-daniel
    container:
      image: bitnami/kubectl
    steps:
      - name: Check out the repository
        uses: actions/checkout@v3
       

     
   
      - name: Set up Kubectl
        run: sudo apt-get update

     
